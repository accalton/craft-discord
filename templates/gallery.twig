{% set images = craft.assets().volume('images').all() %}

{% for image in images %}
    <img src="{{ image.filename|optimize({
        resize: {
            height: 250
        },
        greyscale: true
    }) }}">
{% endfor %}